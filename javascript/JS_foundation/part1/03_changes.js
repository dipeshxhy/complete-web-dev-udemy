let gameName = 'spiderman';

gameName = 'batman';

// console.log(gameName);

const username = 'hiteshdot.com';
username = 'hitesh'; // This will throw an error because username is a constant and cannot be reassigned
